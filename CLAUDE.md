# 项目约定

## 与用户交流

- **始终使用中文回复。** 用户的所有任务，无论内容是中文还是英文，回答一律用中文。
- 提交信息（commit message）、PR 标题和正文也使用中文，与仓库现有历史保持一致。

## 仓库用途

本仓库为 Clink 键盘（iOS）构建 Community 中文语言包（万象词库）。核心约束是
**iOS 键盘扩展的内存上限只有几十 MB**，超出后键盘进程会被系统杀掉，表现为「卡死」。
任何增大 `zh.cime` / `zh.clex` / `zh.cngm` 的改动都必须先评估运行时内存占用。

关键文档：`WANXIANG_V5_COMPACT.md`（体积与崩溃原因）、`CLINK_IME_INVESTIGATION.md`（运行时行为调查）。
