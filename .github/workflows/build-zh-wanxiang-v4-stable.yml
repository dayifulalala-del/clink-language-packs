name: Build and publish Clink Chinese Wanxiang v4 stable

on:
  workflow_dispatch:
  schedule:
    - cron: "41 4 * * *"
  push:
    branches: [main]
    paths:
      - "tools/build_zh_wanxiang_v3.py"
      - "tools/build_zh_wanxiang_v2.py"
      - "source/zh-latin-terms.tsv"
      - "source/zh-domain-terms.tsv"
      - "source/zh-shortcuts.tsv"
      - ".github/workflows/build-zh-wanxiang-v4-stable.yml"

permissions:
  contents: write

concurrency:
  group: clink-zh-wanxiang-v4-stable
  cancel-in-progress: true

jobs:
  build-and-publish:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    env:
      PYTHONUTF8: "1"
      VERSION: vzh-wanxiang-v4-${{ github.run_number }}-${{ github.run_attempt }}

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: Resolve and download current Wanxiang dict-nightly
        id: upstream
        env:
          GH_TOKEN: ${{ github.token }}
          EVENT_NAME: ${{ github.event_name }}
        run: |
          set -euo pipefail
          rm -rf _wanxiang_release _wanxiang_dicts
          mkdir -p _wanxiang_release _wanxiang_dicts

          gh api repos/amzxyz/rime-wanxiang/releases/tags/dict-nightly > _wanxiang_release/release.json
          gh release download dict-nightly \
            --repo amzxyz/rime-wanxiang \
            --pattern base-dicts.zip \
            --dir _wanxiang_release

          python3 - <<'PY'
          import hashlib, json
          from pathlib import Path

          root = Path('_wanxiang_release')
          release = json.loads((root / 'release.json').read_text(encoding='utf-8'))
          asset = next(a for a in release['assets'] if a['name'] == 'base-dicts.zip')
          data = (root / 'base-dicts.zip').read_bytes()
          actual = 'sha256:' + hashlib.sha256(data).hexdigest()
          expected = asset.get('digest')
          print('Wanxiang release id:', release['id'])
          print('Wanxiang published:', release.get('published_at'))
          print('asset digest:', expected)
          print('local digest:', actual)
          if expected and expected != actual:
              raise SystemExit(f'asset digest mismatch: expected {expected}, got {actual}')
          (root / 'current-sha256.txt').write_text(actual + '\n', encoding='utf-8')
          PY

          if [ "$EVENT_NAME" = "schedule" ]; then
            current_digest="$(cat _wanxiang_release/current-sha256.txt)"
            latest_tag="$(gh api "repos/$GITHUB_REPOSITORY/releases/latest" --jq .tag_name 2>/dev/null || true)"
            previous_digest=""

            if [ -n "$latest_tag" ]; then
              rm -rf _previous_release
              mkdir -p _previous_release
              if gh release download "$latest_tag" \
                --repo "$GITHUB_REPOSITORY" \
                --pattern wanxiang-source.json \
                --dir _previous_release >/dev/null 2>&1; then
                previous_digest="$(python3 - <<'PY'
          import json
          from pathlib import Path
          p = Path('_previous_release/wanxiang-source.json')
          try:
              data = json.loads(p.read_text(encoding='utf-8'))
              print(data.get('asset', {}).get('sha256') or '')
          except Exception:
              print('')
          PY
          )"
              fi
            fi

            echo "Current Wanxiang digest:  $current_digest"
            echo "Previous release digest: ${previous_digest:-<none>}"
            if [ -n "$previous_digest" ] && [ "$current_digest" = "$previous_digest" ]; then
              echo "should_build=false" >> "$GITHUB_OUTPUT"
              echo "Wanxiang data unchanged; scheduled release skipped."
              exit 0
            fi
          fi

          echo "should_build=true" >> "$GITHUB_OUTPUT"
          unzip -q _wanxiang_release/base-dicts.zip -d _wanxiang_dicts

      - name: Skip unchanged scheduled build
        if: steps.upstream.outputs.should_build == 'false'
        run: echo "No Wanxiang data change; no new v4 release required."

      - name: Build stable profile
        if: steps.upstream.outputs.should_build == 'true'
        run: |
          set -euo pipefail
          rm -rf Lexicons/zh.clex Lexicons/zh.cime Lexicons/zh.cngm Lexicons/zh.bpevocab Lexicons/zh.mlmodelc
          python3 tools/build_zh_wanxiang_v3.py \
            --dict-root _wanxiang_dicts \
            --code zh \
            --out-dir source \
            --lexicon-limit 600000 \
            --max-ime-readings 600000 \
            --max-candidates 12 \
            --max-word-length 16 \
            --max-following 32 \
            --latin-terms source/zh-latin-terms.tsv \
            --custom-terms source/zh-domain-terms.tsv \
            --shortcut-terms source/zh-shortcuts.tsv \
            --english-limit 20000 \
            --english-typo-limit 2500 \
            --jianpin-limit 30000 \
            --core-compact-reading-budget 400000 \
            --receipt-json source/zh-wanxiang-build.json

      - name: Build Clink files
        if: steps.upstream.outputs.should_build == 'true'
        run: |
          set -euo pipefail
          python3 build-pack.py zh source/zh.txt
          python3 tools/build-ime-table.py zh source/zh-ime.tsv
          python3 tools/validate-pack.py zh
          ls -lh Lexicons/zh.cime Lexicons/zh.clex Lexicons/zh.cngm

      - name: Stable profile regression tests
        if: steps.upstream.outputs.should_build == 'true'
        run: |
          python3 - <<'PY'
          from pathlib import Path

          rows = {}
          for raw in Path('Lexicons/zh.cime').read_text(encoding='utf-8').splitlines():
              fields = raw.split('\t')
              if len(fields) >= 2:
                  rows[fields[0]] = fields[1:]

          exact = {
              'nihao':'你好',
              'dihao':'帝豪',
              'jilidihao':'吉利帝豪',
              'kjzl':'快捷指令',
              'k j z l':'快捷指令',
              'smj':'什么价',
              's m j':'什么价',
              'smjia':'什么价',
              's m jia':'什么价',
              'qujianma':'取件码',
              'qu jian ma':'取件码',
              'xunihao':'虚拟号',
              'xu ni hao':'虚拟号',
              'github':'GitHub',
              'chatgpt':'ChatGPT',
              'appstore':'App Store',
              'appsrore':'App Store',
          }
          failed = []
          for key, want in exact.items():
              got = rows.get(key, [])
              print(key, '=>', ' / '.join(got[:8]) or 'MISSING')
              if not got or got[0] != want:
                  failed.append((key, want, got[:8]))

          contains = {
              'bzd':'不知道',
              'wsm':'为什么',
              'zmhs':'怎么回事',
          }
          for key, want in contains.items():
              got = rows.get(key, [])
              print(key, '=>', ' / '.join(got[:8]) or 'MISSING')
              if want not in got[:8]:
                  failed.append((key, want, got[:8]))

          if len(rows) < 550000:
              failed.append(('IME table unexpectedly small', len(rows)))

          if failed:
              raise SystemExit('v4 stable regression failure: ' + repr(failed))
          print('IME readings:', len(rows))
          PY

      - name: Create Wanxiang source receipt
        if: steps.upstream.outputs.should_build == 'true'
        run: |
          python3 - <<'PY'
          import json
          from pathlib import Path

          release = json.loads(Path('_wanxiang_release/release.json').read_text(encoding='utf-8'))
          build = json.loads(Path('source/zh-wanxiang-build.json').read_text(encoding='utf-8'))
          asset = next(a for a in release['assets'] if a['name'] == 'base-dicts.zip')
          receipt = {
              'profile': 'v4-stable',
              'upstream': 'amzxyz/rime-wanxiang',
              'tag': 'dict-nightly',
              'releaseId': release['id'],
              'publishedAt': release.get('published_at'),
              'targetCommitish': release.get('target_commitish'),
              'asset': {
                  'name': asset['name'],
                  'byteCount': asset['size'],
                  'sha256': asset.get('digest'),
                  'url': asset['browser_download_url'],
              },
              'build': build,
          }
          Path('wanxiang-source.json').write_text(
              json.dumps(receipt, ensure_ascii=False, indent=2), encoding='utf-8')
          PY

      - name: Build Clink release manifest
        if: steps.upstream.outputs.should_build == 'true'
        run: |
          set -euo pipefail
          find Lexicons -mindepth 1 -maxdepth 1 \
            ! -name 'zh.clex' \
            ! -name 'zh.cime' \
            ! -name 'zh.cngm' \
            -exec rm -rf {} +

          rm -rf out
          mkdir -p out
          python3 tools/build-release-manifest.py "$VERSION" "$GITHUB_REPOSITORY" out

          python3 - <<'PY'
          import json
          from pathlib import Path
          m = json.loads(Path('out/manifest.json').read_text(encoding='utf-8'))
          assert [p['code'] for p in m['packs']] == ['zh'], m
          assets = {a['path'] for a in m['packs'][0]['assets']}
          assert assets == {'zh.cime','zh.clex','zh.cngm'}, assets
          print('Manifest verified for zh:', sorted(assets))
          PY

      - name: Publish v4 stable as latest
        if: steps.upstream.outputs.should_build == 'true'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          gh release create "$VERSION" \
            --target "$GITHUB_SHA" \
            --draft \
            --title "Clink 中文万象 v4 stable — $VERSION" \
            --notes "iOS 稳定版：优先中文核心覆盖，降低 CNGM/候选压力；保留简拼、英文、中英混输、纠错、领域词和快捷词。"

          gh release upload "$VERSION" \
            out/assets/zh--zh.cime \
            out/assets/zh--zh.clex \
            out/assets/zh--zh.cngm \
            out/manifest.json \
            wanxiang-source.json

          gh release edit "$VERSION" --draft=false --latest

      - name: Verify published release
        if: steps.upstream.outputs.should_build == 'true'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          rm -rf "$RUNNER_TEMP/release-check"
          mkdir -p "$RUNNER_TEMP/release-check"
          gh release download "$VERSION" --dir "$RUNNER_TEMP/release-check"
          cmp out/manifest.json "$RUNNER_TEMP/release-check/manifest.json"
          cmp wanxiang-source.json "$RUNNER_TEMP/release-check/wanxiang-source.json"
          latest="$(gh api "repos/$GITHUB_REPOSITORY/releases/latest" --jq .tag_name)"
          test "$latest" = "$VERSION"
          echo "Verified latest release: $latest"
