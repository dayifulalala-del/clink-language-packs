# Clink 中文万象 v5 compact

## 为什么选择本仓库后 Clink 会崩溃

v3-31-1 的 zh 包一共 44.9 MB，是官方 zh 包的约 40 倍：

| 文件 | 官方 zh | v3-31-1 | v5 compact |
|---|---|---|---|
| zh.cime | 0.5 MB / 2.8 万 reading | 20.4 MB / 65 万 reading | 2.7 MB / 8.2 万 reading |
| zh.clex | 0.4 MB / 3 万词 | 14.5 MB / 93 万词 | 2.1 MB / 14.9 万词 |
| zh.cngm | 0.4 MB | 9.9 MB（ID 错位） | 1.6 MB |

iOS 对键盘扩展的内存限制只有几十 MB。按普通 Swift 方式把 v3-31-1 的 `zh.cime`
读成 `[String: [String]]`，本机实测仅这一个文件就要约 80 MB，超过限制后键盘进程会被系统直接杀掉。

另外 v3-31-1 的 `zh.cngm` 与 `zh.clex` 词 ID 不一致：CNGM 按 932,800 个词编号，
而 `build-pack.py` 会丢掉带空格或不含字母的词（如 `app store`、`+86`），CLEX 只剩 932,658 个。
于是所有下一词联想都指向错误的词，还有 87 条指向词典末尾之外。

## v5 做了什么

`tools/compact_zh_pack.py` 以已验证的 v3-31-1 资产为输入，只删行、不改顺序：

- 保留每一行原有的候选顺序、每个词原有的 CLEX 词频字节；
- 全部单音节、`source/` 下的快捷词 / 领域词 / 品牌英文、回归用例一律保留；
- 与官方表格式一致：reading 只含 a-z，去掉 `pin yin` 这类带空格的重复键和打不出来的数字键；
- 万象的错音错字纠错 `cuoyin` 全部保留（196 条，如 `maopeifang` → 毛坯房、`zhumulangma` → 珠穆朗玛）。
  这是万象唯一的中文纠错表；`duoyin` 在万象里是「基础词库的扩充」，按普通词参与词频排序。
  Clink 不支持候选注释，所以没有万象那样的正音提示；
- 自维护常见误读 `source/zh-cuoyin-extra.tsv`（如 `zhanshi` 也能出「暂时」、`shuoliao` → 塑料），
  正确的词插在该读音第 5 位，不挤掉原有前 4 个候选；
- 中文 reading 按「能否打出词频前 5 万名的中文词」保留（`--target-rank`），
  英文 / 品牌单词另留 1.2 万个名额；
- 不含英文拼写纠错（如 `githbu` → GitHub），需要时给工具加 `--keep-latin-typos`；
- 多音节行的候选上限按 reading 长度递减（`--candidate-tiers`，默认 ≤6 字母 6 个、
  ≤10 字母 4 个、更长 3 个；单音节保留全部 16 个）；
- `zh.cngm` 按最终 CLEX 的词 ID 重新生成，规则与 `build_zh_wanxiang_v2.py` 相同。

## 拼音按键纠错

参考搜狗（ign/img→ing、uei→ui、uen→un、iou→iu）和 libpinyin（gn↔ng、on↔ong、ue/v↔u）
这类「只纠系统性错误、几乎不误纠」的做法，为全部单音节和最常用的 2 万个词生成纠错键：

| 错误 | 例子 |
|---|---|
| ng 打成 gn / mg | `dign` → 定、`zhognguo` → 中国、`dimg` → 定 |
| ong 漏 g | `zhonguo` → 中国 |
| ao 打成 oa | `nihoa` → 你好 |
| ian / iao / iang / uan / uang 字母颠倒 | `tain` → 天、`xaio` → 小、`gaun` → 关 |
| ju / qu / xu / yu 打成 v | `jv` → 据、`xve` → 学 |
| lüe / nüe 打成 ue | `lue` → 略、`nue` → 虐 |
| 旧拼写 uei / uen / iou | 仅在不产生歧义时生效 |

纠错键如果本身就是合法拼音（如 `liou` = li + ou、`guei` = gu + ei）或已有读音，就不生成，
所以不会干扰正常输入和简拼。和万象、雾凇一样，不开启模糊音（z/zh、n/l 等）。

用官方 zh 词典里 2.5 万个真实高频中文词衡量「前 5 个候选里能打出来」的比例：
v3-31-1 为 93.17%，v5 首版为 92.45%。下面「优化结果」一节改用覆盖面更大的分层评测
（v3-31-1 全量表里 71.8 万个有读音的中文词，按词频分段），结论一致。

## 为什么裁到 10.4 MB 还是会偶发卡死

v5 的前两次发布（`v5-z000001` / `v5-z000002`）把包压到 10.4 MB，不再是必然崩溃，
但仍然会「有的时候」卡死。原因是**真正决定生死的是常驻内存，不是文件大小**。

Clink 把 `zh.cime` 读成 Swift 的 `[String: [String]]`。这种结构里，**一行的固定开销
远大于一个候选**：key 句柄 16 字节 + 数组句柄 16 字节 + 数组 buffer 头 32 字节 ≈ 64 字节，
之后每个候选才 16 字节。所以一个只有 1 个候选的长尾行，成本是那个候选本身的 5 倍。

按这个模型量出的 `v5-z000002`：

| 资源 | 文件 | 常驻估算 | 数量 |
|---|---|---|---|
| zh.cime | 4.3 MB | 17.9 MB | 114,373 行 / 338,970 候选 |
| zh.clex | 3.8 MB | 12.6 MB | 253,253 词 |
| zh.cngm | 2.3 MB | 8.0 MB | 260,410 转移对 |
| **合计** | **10.4 MB** | **38.4 MB** | |

iOS 给键盘扩展的内存上限只有几十 MB，而这个额度**随宿主 App 变化**：在 Safari 开一堆标签、
微信、相机这类本身吃内存的 App 里输入时，留给键盘的份额更小。38.4 MB 的词典加上 Clink
自身的 UI、布局、主题和候选渲染，正好卡在生死线附近——内存宽裕时正常，紧张时被系统杀掉。
这就是「有的时候」的来源。

## v5 的预算怎么重新分配

关键观察：**预算花在行上比花在候选上贵得多，而长尾行恰恰是候选最少的**。
在 v3-31-1 输入上实测每 MB 常驻内存能换到的候选数：

| reading 长度 | 行数 | 候选数 | 每 MB 换到的候选 |
|---|---|---|---|
| 1-2 音节（1-4 字母） | 22,692 | 164,631 | **28,777** |
| 3 音节（5-6 字母） | 20,931 | 57,258 | 18,131 |
| 4 音节（7-8 字母） | 29,688 | 64,199 | 15,666 |
| 6+ 音节（13+ 字母） | 6,607 | 6,929 | **9,273** |

长 reading 吃掉约三分之一内存，只提供 15% 的候选。

所以预算不再按「这一行自己的分数」买，而是按**这一行能不能打出用户真正会输入的词**买：

- `--target-rank 50000`：一个中文 reading 行，只有当它的候选里含已验证 CLEX 词频前 5 万名的
  中文词时才保留。单音节行（401 行）、万象纠错、自维护误读、快捷词、领域词、回归用例一律不受影响；
- `--candidate-tiers 6:6,10:4,24:3`：候选上限按 reading 长度递减。长拼音本身就精确，
  而候选栏一屏也显示不了 10 个。若某个前 5 万名的词恰好排在更深的位置，该行会保留到它为止
  （上限 `--max-candidates`），所以不会因为截断而丢词；
- `--max-latin 12000`（原 30000）：英文 / 品牌词属于中英混输，不属于中文输入体验；
  品牌词和回归词由 `source/` 保护，不参与这个额度；
- `--max-following 6`（原 10）：下一词联想保留 6 个，候选栏本来也显示不完。

拼音按键纠错（`zhognguo` → 中国）**完整保留**，因为打错了还能出对的字属于中文输入体验本身。

## 优化结果

| | v3-31-1 | v5-z000002 | 现在 |
|---|---|---|---|
| 文件合计 | 44.9 MB | 10.4 MB | **6.38 MB** |
| 常驻估算 | ~158 MB | 38.4 MB | **25.6 MB** |
| CIME 行数 | 650,000 | 114,373 | 82,118 |
| CLEX 词数 | 932,658 | 253,253 | 148,861 |
| CNGM 转移对 | — | 260,410 | 175,048 |

体验用「按词频分层，完整拼音打词能否进前 5 候选」衡量，测试集是 v3-31-1 全量表里
71.8 万个有读音的中文词，按 CLEX 词频分段：

| 词频段 | v3-31-1 全量 | v5-z000002 | 现在 |
|---|---|---|---|
| 最高频 1-5 千 | 99.82% | 99.82% | **99.82%** |
| 5 千 - 2 万 | 99.45% | 99.45% | **99.45%** |
| 2 万 - 5 万 | 99.04% | 99.04% | **99.04%** |
| 5 万 - 15 万 | 99.58% | 27.84% | 6.57% |

**前 5 万名高频词一个没少**——这一段正是日常中文输入的实际范围。减少的全部落在 5 万名
以后的长尾，而那一段在 `v5-z000002` 里本来就已经只剩 27.84%。
按「一屏 9 个候选」重测，2 万-5 万段同样是 100.00%，与全量包一致。

## 发布与调参

构建时从万象 `dict-nightly` 下载 `cuoyin.dict.yaml` 作为纠错清单。
推送 `tools/compact_zh_pack.py` 或 `.github/workflows/build-zh-wanxiang-v5-compact.yml`
会自动发布新的 latest（`vzh-wanxiang-v5-z…`，版本号排在所有 v3/v4 之后）。

如果仍然卡死，在 Actions → *Publish compact Clink Chinese Wanxiang (v5)* → Run workflow
里把 **`target_rank`** 调到 35000 或 25000——这是现在最有效的旋钮，它直接决定保留多少行。
实测 `target_rank=40000` 约为 22.4 MB，但 2 万-5 万词频段会开始掉，属于可感知的体验损失，
所以先从 35000 试起；稳定后也可以往上调回 50000 以上。

workflow 会拒绝发布超过 9 MB（或 CIME 超过 4 MB）的包，并且会按上面的模型估算常驻内存，
**超过 30 MB 直接拒绝发布**——这道闸门是这次加的，`v5-z000002` 会被它拦下。

`build-zh-wanxiang-v3.yml` 仍会直接发布未裁剪的 65 万行大包，不要再手动运行它。
